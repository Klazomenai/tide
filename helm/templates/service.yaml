apiVersion: v1
kind: Service
metadata:
  name: {{ include "tide.fullname" . }}
  labels:
    {{- include "tide.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: metrics
      protocol: TCP
      name: metrics
  selector:
    {{- include "tide.selectorLabels" . | nindent 4 }}
